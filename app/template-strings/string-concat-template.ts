import { console } from "console";

const person = {
  name: "Josh",
  age: 38
};

const msg = `${person.name} is ${person.age} years old`;

const msg2 = `
person: ${person.name}
age: ${person.age}
`;

console.log(msg);
console.log(msg2);



